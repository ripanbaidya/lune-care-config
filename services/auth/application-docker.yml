# config for auth - docker profile
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lunecare?currentSchema=auth
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD:}