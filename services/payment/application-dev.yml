# config for payment - dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/payment_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
