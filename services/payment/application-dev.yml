# config for payment - dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lunecare?currentSchema=payment
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
