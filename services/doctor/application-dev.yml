# config for doctor - dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/doctor_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
