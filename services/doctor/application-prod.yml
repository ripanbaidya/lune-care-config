# config for doctor - prod

spring:
  datasource:
    url: ${DOCTOR_DB_URL}
    username: ${DOCTOR_DB_USERNAME}
    password: ${DOCTOR_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 15
      minimum-idle: 3
  jpa:
    show-sql: false
