# config for patient - dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/patient_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
