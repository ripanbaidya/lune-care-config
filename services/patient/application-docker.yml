# config for patient - docker profile
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lunecare?currentSchema=patient
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}