# config for appointment - dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lunecare?currentSchema=appointment
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379
